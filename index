<!DOCTYPE html>
<html lang="si">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>නවක සිසුවෙකු ලියාපදිංචි කිරීම</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Bootstrap CDN - Bootstrap request එක නිසා ඇතුළත් කළා. නමුත් මෙහි Tailwind CSS ප්‍රධාන වශයෙන් භාවිතා කරයි. -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script>
        // Tailwind CSS වලට අකුරු මෝස්තරය (font-family) එක් කිරීම
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    },
                },
            },
        };
    </script>
    <style>
        /* පොප්-අප් කාඩ් එක සඳහා සජීවීකරණය */
        @keyframes fadeInScale {
            from {
                opacity: 0;
                transform: scale(0.8);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }
        
        @keyframes pulseShadow {
            0% {
                box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
            }
            50% {
                box-shadow: 0 15px 45px rgba(0, 0, 0, 0.2);
            }
            100% {
                box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
            }
        }

        .popup-card {
            animation: fadeInScale 0.5s ease-out forwards, pulseShadow 3s infinite ease-in-out;
        }

        /* ඉමෝජි සඳහා සජීවීකරණය */
        @keyframes emojiJump {
            0%, 100% {
                transform: translateY(0) rotate(0deg);
            }
            50% {
                transform: translateY(-10px) rotate(5deg);
            }
        }

        .animated-emoji {
            display: inline-block;
            animation: emojiJump 1s infinite alternate;
        }
    </style>
</head>
<body class="bg-blue-100 flex items-center justify-center min-h-screen font-sans">

    <!-- පොප්-අප් කාඩ් එක සඳහා පසුබිම් ආවරණය (overlay) -->
    <div id="overlay" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 transition-opacity duration-500 opacity-0 hidden">
        
        <!-- පොප්-අප් කාඩ් එක -->
        <div id="popup" class="popup-card bg-white rounded-3xl shadow-lg p-8 max-w-sm text-center transform scale-0">
            <h2 class="text-4xl font-extrabold text-blue-800 mb-4">
                👋 Welcome!
            </h2>
            <p class="text-lg text-gray-700 mb-6">
                Register new student
            </p>
            <div class="text-6xl mb-6 flex justify-center space-x-4">
                <span class="animated-emoji">📚</span>
                <span class="animated-emoji" style="animation-delay: 0.2s;">✏️</span>
                <span class="animated-emoji" style="animation-delay: 0.4s;">🎓</span>
            </div>
            <button onclick="redirectToGoogle()" class="w-full bg-gradient-to-r from-blue-600 to-purple-600 text-white font-semibold py-3 px-6 rounded-full shadow-lg hover:from-blue-700 hover:to-purple-700 transition duration-300 transform hover:scale-105 focus:outline-none focus:ring-4 focus:ring-blue-300 focus:ring-offset-2">
                Google වෙත යන්න
            </button>
        </div>
    </div>

    <!-- JavaScript කේතය -->
    <script>
        /**
         * මෙම ශ්‍රිතය Google.com වෙත යොමු කරයි.
         */
        function redirectToGoogle() {
            window.location.href = 'https://script.google.com/macros/s/AKfycbyMtfhCjQcm3Q0VVppH8KGaeGXxU313xIBbMsrBxf9JMug3xHMfApqUyV4yAZ14t6rM/exec';
        }

        /**
         * පිටුව සම්පූර්ණයෙන් පූරණය වූ විට පොප්-අප් එක පෙන්වයි.
         */
        window.onload = function() {
            const overlay = document.getElementById('overlay');
            const popup = document.getElementById('popup');
            
            // පොප්-අප් එක පෙනෙන ලෙස සකස් කිරීම
            overlay.classList.remove('hidden');
            setTimeout(() => {
                overlay.classList.remove('opacity-0');
                popup.classList.remove('scale-0');
            }, 10);
        };
    </script>

</body>
</html>
